#include "symboltable.hpp"


//This is how I feel that the cpp would start off looking like,
//some way to insert each and print them out. 
//And then using push back to put them into a table.
//It's not compiling because I can't get the logic
//quite right. 


/*
// Function to insert a variable into the symbol table
bool SymbolTable::insertVariable(const Variable& variable) {
    variable.push_back(variable);
    return true; 
}

// Function to insert a function into the symbol table
bool SymbolTable::insertFunction(const Function& function) {
    function.push_back(function);
    return true; 
}

// Function to insert a procedure into the symbol table
bool SymbolTable::insertProcedure(const Procedure& procedure) {
    procedure.push_back(procedure);
    return true; 
}

// print a variable
void Variable::print() const {
    cout << "Variable: " << name << ", Type: " << type << ", Scope: " << scope << endl;
}

// print out a function
void Function::print() const {
    cout << "Function: " << name << ", Return Type: " << returnType << ", Scope: " << scope << endl;
}

//print out a procedure
void Procedure::print() const {
    cout << "Procedure: " << name << ", Scope: " << scope << endl;
}
*/